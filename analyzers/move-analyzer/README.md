# Move Analyzer

This directory will hold the isolated Move source analyzer container. The MVP implements the initial Sui source heuristics in `packages/adapters/sui`; containerized analyzer execution is the next hardening step.
